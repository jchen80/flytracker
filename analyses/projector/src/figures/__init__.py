"""figures: projector plotting CLIs.

Thin command-line entry points, one per analysis category, plus shared
scaffolding in ``plotting_helpers``. Plotting implementations live in the sibling
``putil`` package. Run e.g.::

    python -m analyses.projector.src.figures.generate_pursuit_distributions <dir>
"""
