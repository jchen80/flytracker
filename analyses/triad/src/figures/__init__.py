"""figures: triad plotting CLIs.

Thin command-line entry points (one per analysis category) plus shared
scaffolding in ``plotting_helpers``. The plotting implementations live in the
sibling ``putil`` package. Run a CLI with e.g.::

    python -m analyses.triad.src.figures.generate_pursuit_metric_plots <rootdir>
"""
